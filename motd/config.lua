config = {}

---------------------------------------------------------------------
-- THERE IS ANOTHER IMPORTANT CONFIGURATION FILE IN html/config.js --
---------------------------------------------------------------------

config.command = 'motd'                 -- Command in Chat (without slash)
config.openOnSpawn = false               -- Should the MOTD open when the player spawns in
config.openOnSpawnOnce = false          -- Should we only open the menu once per script start (If so disable the above action)


